# app { #modal.app }

`app`

## Attributes

| Name | Description |
| --- | --- |
| [App](#modal.app.App) |  |
| [container_app](#modal.app.container_app) |  |

## Functions

| Name | Description |
| --- | --- |
| [is_local](#modal.app.is_local) | Returns if we are currently on the machine launching/deploying a Modal app |

### is_local { #modal.app.is_local }

`app.is_local()`

Returns if we are currently on the machine launching/deploying a Modal app

Returns `True` when executed locally on the user's machine.
Returns `False` when executed from a Modal container in the cloud.